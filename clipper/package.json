{
  "name": "bruv-clipper",
  "version": "0.1.0",
  "private": true,
  "description": "BRUV web clipper — capture posts into BRUV cards and slide decks",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.3"
  }
}
